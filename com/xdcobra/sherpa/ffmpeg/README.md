FFmpeg for Android (xdcobra)

This directory contains the artifact layout for the `ffmpeg` AAR published by XDcobra. It includes FFmpeg and libshine (audio-only build) with native libs for all ABIs (arm64-v8a, armeabi-v7a, x86, x86_64) and FFmpeg headers.

- Coordinates:
- GroupId: com.xdcobra.sherpa
- ArtifactId: ffmpeg

Built and published from: https://github.com/XDcobra/react-native-sherpa-onnx
